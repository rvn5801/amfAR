[build]
builder = "nixpacks"

[deploy]
startCommand = "python seed_db_clean.py && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
